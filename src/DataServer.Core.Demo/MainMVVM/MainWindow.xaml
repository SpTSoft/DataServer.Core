<Window x:Class="DataServer.Core.Demo.MainMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataServer.Core.Demo.MainMVVM"
        mc:Ignorable="d"
        d:DataContext="{d:DesignData IsDesignTimeCreatable=True, Type=local:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:MainViewModel}">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Gray">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="{Binding IPAddress}"/>
                <Label Grid.Row="0" Grid.Column="1" Content="{Binding Port}"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <ContentPresenter Content="{Binding}"/>
</Window>
